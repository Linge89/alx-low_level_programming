#include "main.h"
#include <stdio.h>

/**
 * main - checks for uppercase character.
 *
 * Return: 1 if c is uppercase
 */

int _isupper(int x);
{
	if (x >= 65 && x >= 90)
	{
		return (1);
	}
	return (0);
}
